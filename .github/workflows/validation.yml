name: Diagnostic final des dépendances

on:
  pull_request:

permissions:
  contents: write

env:
  NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
  NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_validation_locale
  APP_URL: http://localhost:3000

jobs:
  diagnostiquer:
    name: Capturer l’audit après mises à jour
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Récupérer la branche de travail
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Régénérer le verrouillage corrigé
        run: npm install --no-audit --no-fund

      - name: Générer l’audit complet
        run: npm audit --json > audit-complet.json || true

      - name: Enregistrer le diagnostic et le verrouillage
        run: |
          rm -f lint-result.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A package-lock.json audit-complet.json lint-result.txt
          if ! git diff --cached --quiet; then
            git commit -m "chore: actualiser le diagnostic final temporaire"
            git push origin HEAD:${{ github.head_ref }}
          fi
